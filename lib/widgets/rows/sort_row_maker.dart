import 'package:flutter/material.dart';

class SortRowMaker extends StatelessWidget {
  List<String> btnTitle;

  SortRowMaker({Key? key, required this.btnTitle}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
